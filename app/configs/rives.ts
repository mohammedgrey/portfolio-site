const riv = (path: string) => "/assets/rives/" + path;

const rives = {
  switches: {
    darkLight: riv("darkLight.riv"),
  },
  loading: riv("loading.riv"),
  input: riv("input.riv"),
};

export default rives;
